node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local SEO / verification scaffolding — never part of the site
log-*.txt
verify-*.txt
verify-*.ps1
run-verify.ps1
findings-*.txt
git-check.txt
commit-msg.txt

# Reference data kept locally (source files for state_data.json calibration) and
# exported analytics reports — not site assets, not for the repo.
/data/
mortgagepro.io-Performance-on-Search-*.zip
